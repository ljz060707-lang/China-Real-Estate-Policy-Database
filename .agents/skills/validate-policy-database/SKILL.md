---
name: validate-policy-database
description: Validate policy database counts, hashes, source lineage, missingness, duplicates, date ranges, geographic matches, classifications, DuckDB views, and research panels.
---
# Validate policy database
Read `references/checks.md`, run `scripts/run.py`, and reject a release when hard checks fail. Report warnings without silently repairing source data.
