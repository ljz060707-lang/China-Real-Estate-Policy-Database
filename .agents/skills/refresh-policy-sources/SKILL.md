---
name: refresh-policy-sources
description: Refresh reviewed policy source registries idempotently, save raw HTML/PDF snapshots, detect unchanged hashes, create versions, and preserve the last valid database on failure.
---
# Refresh policy sources
Read `references/safety.md`. Use only enabled registry entries. Discover, fetch, snapshot, parse, normalize, validate, then atomically publish. Record every run and queue ambiguity.
