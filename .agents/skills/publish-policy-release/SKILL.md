---
name: publish-policy-release
description: Publish a validated, versioned, hashed, citable policy database snapshot with Parquet, CSV, Excel, documentation, manifests, quality reports, and Git metadata.
---
# Publish policy release
Read `references/release.md`. Run validation first, then `scripts/run.py <version>`. Verify every manifest hash and include a reproducible citation. Never mutate a published version.
