# 数据治理守则

- 原始数据永不覆盖；Raw 文件只读保存并登记 SHA-256。
- 缺失信息不得猜测；不确定值保持为空并进入人工审核。
- 标准化值和原始值同时保存，任何记录可追溯到文件、工作表、行和单元格。
- 所有自动分类必须保存证据、置信度、方法和审核状态。
- 所有数据库写入必须先经过模型、枚举和质量规则验证。
- 派生表不得反向覆盖主数据；统计结果必须来自 Curated 层。
- 变更数据模型时必须同步更新数据字典、迁移脚本和测试。
- 任何发布必须生成验证报告、清单、数据哈希、版本和创建时间。
- 增量更新必须幂等，不覆盖历史版本；失败不得破坏现有数据库。
