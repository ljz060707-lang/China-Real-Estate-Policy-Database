# 兼容说明：本文件不再保存来源事实，也不会被抓取器读取。
# 唯一权威来源登记：data/reference/source_registry.yaml（schema_version=2）。
# 请使用 `policydb sources validate-registry`、`matrix` 和 `export-audit` 管理与审计。
deprecated: true
canonical_registry: data/reference/source_registry.yaml
schema_version: 2
